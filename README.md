# Função `print()` em Python 🖨️

A função `print()` é uma ferramenta embutida em **Python**, usada para exibir informações na tela durante a execução do código. Ela é essencial para mostrar resultados, mensagens e valores de variáveis, sendo um dos primeiros recursos que programadores aprendem ao iniciar na linguagem.

---

## Tecnologias utilizadas

- **Python 3**  
- Função `print()` da biblioteca padrão  
- Saída via console  

---

## Sintaxe e parâmetros

A função `print()` possui uma sintaxe flexível com os seguintes parâmetros:

- `*objects`: valores a serem exibidos  
- `sep`: separador entre os valores (padrão é espaço)  
- `end`: define o final da linha (padrão é quebra de linha)  
- `file`: destino da saída (padrão é a tela)  
- `flush`: força a exibição imediata  

---

## Exemplos de uso

A função pode ser usada para:

- Exibir texto simples: `print('Olá, Mundo!')`  
- Mostrar números: `print(10)`  
- Imprimir múltiplos valores: `print('Ricardo', 32)`  
- Personalizar saída:  
  - `print('A', 'B', sep='-')`  
  - `print('Linha 1', end=' ')`  

---

## Conceitos de programação aplicados

- **Sintaxe básica**: prática com estrutura da linguagem  
- **Lógica e criatividade**: construção de mensagens e testes  
- **Documentação**: uso junto com comentários (`#` ou `"""..."""`)  
- **Organização de código**: facilita a leitura e depuração  

---

## Importância

Dominar a função `print()` é um passo fundamental para qualquer desenvolvedor Python. Ela é usada em praticamente todos os projetos iniciais e continua sendo útil em fases avançadas para testes, logs e interações com o usuário.
